# `sheldon` configuration file
# ----------------------------
#
# You can modify this file directly or you can use one of the following
# `sheldon` commands which are provided to assist in editing the config file:
#
# - `sheldon add` to add a new plugin to the config file
# - `sheldon edit` to open up the config file in the default editor
# - `sheldon remove` to remove a plugin from the config file
#
# See the documentation for more https://github.com/rossmacarthur/sheldon#readme

shell = "zsh"

[plugins]

# Completion directories must be on fpath before compinit.
[plugins.mise-completion]
local = "~/.local/share/zsh/site-functions"
apply = ["fpath"]

[plugins.mise-synced-completions]
local = "~/.local/share/mise-completions/zsh"
apply = ["fpath"]

[plugins.zsh-completions]
github = "zsh-users/zsh-completions"
dir = "src"
apply = ["fpath"]

[plugins.compinit]
inline = '''
autoload -Uz compinit
compinit
'''

# zoxide and atuin require compinit to have run first.
[plugins.zoxide]
inline = 'eval "$(zoxide init zsh --cmd j)"'

[plugins.atuin]
inline = 'eval "$(atuin init zsh)"'

# fzf-tab must load after compinit and before widget-wrapping plugins.
[plugins.fzf-tab]
github = "Aloxaf/fzf-tab"

[plugins.zsh-ssh]
github = "sunlei/zsh-ssh"

[plugins.deja]
inline = '''
# Keep Tab for fzf-tab and use Ctrl+Tab for deja alternatives.
export DEJA_CYCLE_KEY='^[[9;5u'
# Preserve the Emacs Ctrl+X prefix.
export DEJA_TOGGLE_KEY=

if [[ -r "$HOME/.local/share/deja/init.zsh" ]]; then
  source "$HOME/.local/share/deja/init.zsh"
else
  eval "$(deja init zsh)"
fi
'''

[plugins.powerlevel10k]
github = "romkatv/powerlevel10k"

[plugins.forgit]
github = "wfxr/forgit"

[plugins.dir-meow]
github = "Elliot-32/dir-meow"

[plugins.oh-my-zsh-plugins]
github = "ohmyzsh/ohmyzsh"
dir = "plugins"
use = [
  "sudo/sudo.plugin.zsh",
  "extract/extract.plugin.zsh",
]

# Keep syntax highlighting last among plugins.
[plugins.zsh-syntax-highlighting]
github = "zsh-users/zsh-syntax-highlighting"

# For example:
#
# [plugins.base16]
# github = "chriskempson/base16-shell"
